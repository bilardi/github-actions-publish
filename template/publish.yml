name: Publish posts
on:
  workflow_dispatch:
jobs:
  publish:
    uses: {{PUBLISH_REPO}}/.github/workflows/publish.yml@{{LATEST_TAG}}
    secrets:
      MASTODON_ACCESS_TOKEN: ${{ secrets.MASTODON_ACCESS_TOKEN }}
      BUFFER_ACCESS_TOKEN: ${{ secrets.BUFFER_ACCESS_TOKEN }}
      DEV_TO_API_KEY: ${{ secrets.DEV_TO_API_KEY }}
